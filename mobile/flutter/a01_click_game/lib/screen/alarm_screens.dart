import 'package:flutter/material.dart';

class AlarmScreens extends StatelessWidget {
  const AlarmScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('알림 탭 (예시 페이지)'));
  }
}
